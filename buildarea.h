#include <stdio.h>
#include <vector>
#include <string>
#include <fstream>
#include <iostream>

std::vector<std::vector<char>> readFile(std::string inFile);

void printArea(std::vector<std::vector<char>> area);