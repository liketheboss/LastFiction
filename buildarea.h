#include <stdio.h>
#include <vector>
#include <string>
#include <fstream>
#include <iostream>

using namespace std;

vector<vector<char>> readFile(string inFile);

void printArea(vector<vector<char>> area);